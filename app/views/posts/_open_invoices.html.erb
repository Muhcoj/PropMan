<% open_invoices.each do |open_invoice|%>
	<div class='invoice col-md-3'>
		<h4>
			<%= open_invoice.user.full_name %>
		</h4>
		<p>
			<span class='invoice-details'>Date:</span> <%= open_invoice.year %> <%= open_invoice.year %>
		</p>
		<p>
			<span class='invoice-details'>Open invoice:</span> € <%= open_invoice.payment_due %>
		</p>

		<div class='row'>
			<div class='col-md-6 column'>
				<!-- TODO LINK MAILING -->
				<%= link_to 'Reminder', posts_path, class: 'btn btn-warning btn-block' %>
				<!-- TODO LINK MAILING -->
			</div>
			<div class='col-md-6 column'>
				<%= link_to 'Paid', paid_finance_path(open_invoice), class: 'btn btn-success btn-block', id: "paid_#{open_invoice.id}" %>
			</div>
		</div>
	</div>
<% end %>